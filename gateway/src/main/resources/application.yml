server:
  port: 8080

spring:
  application:
    name: nerabbs-gateway

  data:
    redis:
      host: redis
      port: 6379
      timeout: 10s

  cloud:
    consul:
      host: consul
      port: 8500

sa-token:
  token-name: nerabbs

nerabbs:
  frontend:
    url: http://frontend:3000