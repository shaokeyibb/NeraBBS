spring:
  application:
    name: nerabbs-service-user

  cloud:
    consul:
      host: consul
      port: 8500

    config:
      discovery:
        enabled: true
        serviceId: nerabbs-middleware-config