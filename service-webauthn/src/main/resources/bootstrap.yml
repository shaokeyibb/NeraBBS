spring:
  application:
    name: nerabbs-service-webauthn

  cloud:
    consul:
      host: consul
      port: 8500

    config:
      discovery:
        enabled: true
        serviceId: nerabbs-middleware-config